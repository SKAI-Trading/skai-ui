/**
 * SKAI UI Icons
 *
 * Re-exports all icons from lucide-react through skai-ui.
 * Import icons from '@skai/ui/icons' instead of 'lucide-react' directly.
 *
 * @example
 * ```tsx
 * import { Bot, TrendingUp, Wallet } from '@skai/ui/icons';
 * ```
 */
export * from "lucide-react";
