# SPLIT — this file is intentionally empty of rows.
#
# It covered predict AND play, so it resolved to no section and its 60 rows
# applied to zero frames for two waves. The rows now live, verbatim, in:
#     status.wave2.predict.tsv  (33 rows)
#     status.wave2.play.tsv     (27 rows)
#
# Kept as a tombstone rather than deleted so the provenance of those two
# files is discoverable from the name the lane actually wrote.
