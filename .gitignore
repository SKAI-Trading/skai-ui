node_modules/
dist/
# dist held aside while the install-time build runs (scripts/ensure-dist.js)
.dist-prepare-backup/
.turbo/
coverage/
*.log
.DS_Store
*.local
.env
.env.*
!.env.example
storybook-static/

# Figma token exports (regenerated from Figma)
design-tokens.figma.json
